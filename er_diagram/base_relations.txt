users (username (pk), password)
edited_drinks (update_id (pk), username (fk), drink_code (fk))
drinks_menu (drink_code (pk), name, description, availability, price)
edited_tapas (update_id (pk), username (fk), tapas_code (fk))
tapas_menu (tapas_code (pk), name, description, availability, price)
handled_bookings (update_id (pk), username (fk), booking_num (fk))
bookings (booking_num (pk), customer_num(fk), message, booked_date, booked_time, current_date, amount_of_people, confirmed)
customers (customer_num(pk), firstname, surname, email)
review (message_id(pk), customer_num(fk), current_date)